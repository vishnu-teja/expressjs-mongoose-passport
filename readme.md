Express Starter app with mongoose and passport authentication

================================================================

1. download the package
2. change db name in './config/db-config.js'
3. change the secretKey in './config/passport-config.js'
4. run npm install
